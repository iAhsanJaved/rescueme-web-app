@layout('layouts.app')

@section('content')
  @set('title', 'View Device')
  @set('mainTitle', 'Devices')
  @set('subtitle', 'View Device')
  @include('dashboard.partials.header')

  <div class="container-fluid mt--7">
    <div class="row">
      <div class="col-xl-12 order-xl-1">
        <div class="card bg-secondary shadow">
          <div class="card-header bg-white border-0">
            <div class="row align-items-center">
              <div class="col-8">
                <h3 class="mb-0">{{ subtitle }}</h3>
              </div>
              <div class="col-4 text-right">
                <a href="{{ route('devices.index') }}" class="btn btn-sm btn-primary">Back to list</a>
              </div>
            </div>
          </div>
          <div class="card-body">

            <div class="pl-lg-4">
              
              
              @each(translation in device.languages)
              <div class="form-group row">
                <label class="col-md-3 col-form-label form-control-label">Name in {{ translation.name }} </label>
                  <div class="col-md-8">
                    <input class="form-control"  type="text" value="{{ translation.pivot.name }}" disabled>
                  </div>
              </div>
              @endeach
              @each(translation in device.languages)
              <div class="form-group row">
                <label class="col-md-3 col-form-label form-control-label">Description in {{ translation.name }} </label>
                  <div class="col-md-8">
                    <input class="form-control"  type="text" value="{{ translation.pivot.description }}" disabled>
                  </div>
              </div>
              @endeach
              
              <div class="form-group row">
                <label class="col-md-3 col-form-label form-control-label">Status</label>
                  <div class="col-md-8">
                    <input class="form-control"  type="text" value="{{ device.status ? 'Enabled' : 'Disabled' }}" disabled>
                  </div>
              </div>

              <div class="form-group row">
                <label class="col-md-3 col-form-label form-control-label">Location</label>
                  <div class="col-md-8">
                    <input class="form-control"  type="text" value="{{ (getMatchedTranslation(device.location.languages, "en")).pivot.name }}" disabled>
                  </div>
              </div>

              <div class="form-group row">
                <label class="col-md-3 col-form-label form-control-label">Organization</label>
                  <div class="col-md-8">
                    <input class="form-control"  type="text" value="{{ (getMatchedTranslation(device.organization.languages, "en")).pivot.name }}" disabled>
                  </div>
              </div>

              <div class="form-group row">
                <label class="col-md-3 col-form-label form-control-label">Created On</label>
                  <div class="col-md-8">
                    <input class="form-control"  type="text" value="{{ device.created_at }}" disabled>
                  </div>
              </div>

              <div class="form-group row">
                <label class="col-md-3 col-form-label form-control-label">Modified On</label>
                  <div class="col-md-8">
                    <input class="form-control"  type="text" value="{{ device.updated_at }}" disabled>
                  </div>
              </div>

            </div>
          </div>
        </div>
      </div>
    </div>

    @include('layouts.footers.auth')
  </div>
@endsection
